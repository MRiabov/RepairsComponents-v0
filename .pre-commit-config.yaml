repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.6.9   # Use the latest Ruff version
    hooks:
      - id: ruff-format
      - id: ruff
        name: ruff (imports only)
        args: [--select, I, --select, F401, --fix]
